You are a clinical decision classifier.

Classify the following physician decision.

Decision:
{decision}

Step 1 — assign type:
- medication: the decision is about ordering, stopping, or switching a drug
- procedure: a procedural or surgical intervention performed on the patient
- diagnosis: the physician establishes or confirms a specific clinical diagnosis that drives subsequent management
- plan: any other attending-level management decision not covered above

Step 2 — if type is medication, assign action:
- start: a drug is newly ordered
- stop: a drug is held, withheld, or discontinued
- switch: one drug is replaced by a different drug

Action must be one of start, stop, or switch when type is medication. Never null.

Output JSON only:
{{"type": "medication"|"procedure"|"diagnosis"|"plan", "action": "start"|"stop"|"switch"|null}}

Set action to null only if type is not medication.
