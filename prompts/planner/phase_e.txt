## Phase E — Final output

Output the final plan as a single JSON object. No markdown fences, no extra text — JSON only.

CRITICAL: The APPROVED STAGE blocks at the end of Phase D are the authoritative source of truth. Copy those stage boundaries and GT items verbatim into the JSON below. Do NOT re-derive, re-select, or modify any GT item. The Phase D approved plan is final — Phase E is only a formatting step.

Mechanical constraint — every GT item with a numeric `index` MUST satisfy: index > stage end. If the approved plan has Stage N ending at X and a GT item at index X, the stage end must be X-1 (not X). Do not output a stage where index == end or index < end.

GT item format — use `type` to classify the decision:
- {"type": "diagnosis", "icd_code": "...", "icd_version": 9|10, "display": "..."}
- {"type": "procedure", "index": N, "icd_code": "...", "icd_version": 9|10, "display": "..."}
- {"type": "medication", "action": "start", "index": N, "drug": "..."}  ← new drug; index required
- {"type": "medication", "action": "stop", "drug": "..."}               ← drug held/discontinued; no index
- {"type": "medication", "action": "increase_dose", "drug": "..."}      ← dose increase; no index
- {"type": "medication", "action": "decrease_dose", "drug": "..."}      ← dose decrease; no index
- {"type": "plan", "section": "...", "span": "..."}

Do NOT use plan GT for medication decisions — always use the medication type with the appropriate action.

The `type` field determines which submit tool the model receives and how its submission is scored. Do not add a `source` field.

available_agents rules — list only the agents whose information is essential for the doctor to make the decisions required at this stage:
- "patient": include when direct patient history or symptom questioning is needed (typically Stage 1 and any stage where new patient-reported symptoms drive the decision)
- "nurse": include when bedside vitals, monitoring data, fluid balance, or administered medications are needed
- "lab": include when laboratory results or diagnostic imaging are needed
- "history": include when prior admission records would meaningfully inform this stage's decisions

{
  "stages": [
    {
      "label": "generic admission phase only — use temporal position, not clinical content. Examples: 'Initial Presentation', 'Inpatient Management', 'Discharge Planning'. Never include diagnoses, procedures, drugs, or findings.",
      "index_range": [start, end],
      "trigger": {"agent": "patient"|"nurse", "context": "one sentence: the clinical fact that opens this stage"},
      "available_agents": ["patient"|"nurse"|"lab"|"history"],
      "gt": [
        {"type": "diagnosis", "icd_code": "...", "icd_version": 9, "display": "..."},
        {"type": "procedure", "index": N, "icd_code": "...", "icd_version": 9, "display": "..."},
        {"type": "medication", "action": "start", "index": N, "drug": "..."},
        {"type": "medication", "action": "stop", "drug": "..."},
        {"type": "plan", "section": "...", "span": "..."}
      ]
    }
  ]
}

Trigger rules:
- Stage 1: agent = "patient", context = one-sentence factual summary of the presenting complaint and key history from the HPI. Do NOT include diagnoses or findings — describe only symptoms and reason for presentation.
- Later stages: agent = "nurse", context = one-sentence description of the clinical event or procedure that closed the previous phase (e.g. "the endoscopy procedure has been completed" or "the patient has been stabilized and transferred out of the ICU"). CRITICAL: describe ONLY what happened (the event/procedure/transition), NEVER the result, finding, or diagnosis. The MUT must discover findings by querying agents — do not give them away in the trigger.
Write only clinical facts. Do not write dialogue. The agent will use this context to speak in their own voice.
