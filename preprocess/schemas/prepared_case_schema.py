"""Prepared data schema definition.
Only responsible for defining the field structure and validation entry point of prepared data.
"""


def validate_prepared_case(payload) -> bool:
    """Validate if the prepared case structure complies with the convention."""
    raise NotImplementedError
